import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.tablaIdiomasNinos',
  appName: 'tablaIdiomasNinos',
  webDir: 'www'
};

export default config;
